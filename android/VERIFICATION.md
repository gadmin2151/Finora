# 27G Finora 1.6.0 — длинный чек, проверка 26 сентября 2026

Подписанный APK: `finora-1.6.0.apk`, versionCode `11`, application ID `work.gadmin.finora`.

SHA-256 APK: `1e8cf75257b12f0a3f94609ac36a96d8f90a5d47715f707207d941272129aa64`.

- Режим **Добавить → Длинный чек** автоматически снимает перекрывающиеся фрагменты при движении сверху вниз и собирает один JPEG на телефоне. Пауза, подсказки при потере совпадения, прокручиваемый просмотр, увеличение и пересъёмка включены.
- Снимок попадает в черновик только после **Использовать**. Распознавание и подтверждение товаров/итога остаются отдельными действиями; автоматического создания расхода нет.
- **30 Android unit-тестов**, Kotlin format, release lint, R8 и проверка трёх ML Kit-конструкторов прошли. `apksigner verify` подтверждает прежний сертификат SHA-256: `3a3c14d603cc9cfeff5092ece5101acc60512787e90f1330292d98e4f45ee6c0`.
- На Android 15 эмуляторе прошли **4 проверки**: сшивка с контролем положения печатных строк, отбрасывание неподвижных/пустых кадров, сохранение последнего небольшого фрагмента и длинного черновика; сшивка фрагментов частного примера Linella; CameraX → автоматический захват → просмотр → файл для черновика. Реальная фотография и её фрагменты в репозиторий не включены.
- Проверки выявили неоднозначность повторяющихся строк и погрешность вертикального масштаба. Сопоставление нескольких вариантов и отдельный коэффициент по вертикали исправили эти случаи; повторный прогон прошёл с проверкой пикселей печатных строк. Обычная камера и QR также прошли отдельную проверку открытия.
- Сервер сохраняет разрешение длинной ленты и подаёт AI перекрывающиеся детальные фрагменты вместе с оригиналом. **200 тестов и 11 subtests**, Ruff lint/format прошли. Новых миграций и изменений API нет.
- Физический телефон недоступен по прежнему адресу Wi-Fi ADB (`Connection refused`). Установка 1.6.0 и съёмка движущейся камерой на физическом устройстве **не подтверждены**. Эмулятор и сшивка подготовленных фрагментов не заменяют такую проверку.

[Как снимать и ограничения режима](../docs/LONG-RECEIPTS.md).

---

# 27G Finora 1.5.0 — проверка 26 сентября 2026

Подписанный APK: `finora-1.5.0.apk`, versionCode `10`, application ID `work.gadmin.finora`.

SHA-256 APK: `9860ffa37272baf7d2c3367133e6dd954c6c5bb07a82eb79d1a658f081cc0c98`.

- 22 unit tests, Kotlin format, release lint, R8 и проверка ML Kit-конструкторов — успешно.
- `apksigner verify` подтверждает прежний сертификат SHA-256: `3a3c14d603cc9cfeff5092ece5101acc60512787e90f1330292d98e4f45ee6c0`. Обновление не требует удаления приложения.
- Новый нативный AI-чат: история, быстрые отчёты, уточняющие вопросы, ссылки на исходные чеки; API-модели проверены на совместимость и слияние истории без дубликатов.
- На отдельном Android 15 эмуляторе прошёл Compose-сценарий: отчёт → ввод вопроса с открытой клавиатурой → обновление состояния → черновик сохранён, кнопка отправки доступна.
- Первый снимок тестового экрана выявил отсутствие фонового Surface в тестовой оболочке; оболочка приведена к теме приложения. Для захвата с открытой клавиатурой используется UI Automation: PixelCopy в headless-эмуляторе ожидал поверхность до таймаута. Повторный UI-прогон прошёл.
- Физический Pixel 8 был недоступен по прежнему адресу Wi-Fi ADB (`Connection refused`). Установку 1.5.0 и повторную проверку физического QR на нём этот отчёт **не подтверждает**. Стабильность предыдущей 1.4 не заменяет такую проверку.
- [Android CI](https://github.com/gadmin2151/Finora/actions/runs/36196157660) и [server CI](https://github.com/gadmin2151/Finora/actions/runs/36196157639) для `8e6db93` прошли. Сервер обновлён до этой версии.
- [Подписанный APK 1.5.0](https://finora.gadmin.work/assets/mobile/finora-1.5.0.apk) опубликован по HTTPS. Скачанная обратно копия размером **24 462 135 байт** совпала по SHA-256 с локальным подписанным APK. Это подтверждает публикацию, но не установку на физический телефон.

[Серверный аудит и распознавание](../docs/AUDIT-2026-09.md). Ниже — история предыдущих выпусков, её результаты относятся только к указанным версиям.

---

# 27G Finora 1.4.0 — проверка 25 сентября 2026

Подписанный APK: `finora-1.4.0.apk`, versionCode `9`, application ID `work.gadmin.finora`.

SHA-256 APK: `63e94067a0e6a41fe74fd8cad6b66d159badf52335cc93c8a0a70e08ca013810`.

- Выбранный пользователем кошелёк-оригами: launcher, общий логотип, экран добавления и загрузка. Тёмная нефритовая палитра с мятой и золотыми акцентами.
- Обновление данных жестом сверху вниз на основных вкладках и карточке чека; есть доступная кнопка обновления и время успешного обновления.
- Фотография профиля: защищённая загрузка/удаление, кэш и инициалы при отсутствии снимка.
- 19 unit tests, Kotlin format, release lint, R8, сохранность ML Kit reflection-конструкторов и `apksigner verify` прошли.
- Подпись осталась прежней; установка выполняется поверх существующего приложения.

## Установка и устройство

- Финальный APK установлен поверх прежнего на Pixel 8 / Android 17 по Wi-Fi ADB. Авторизация сохранена. Значок кошелька проверен на рабочем столе; главный экран соответствует выбранному стилю.
- Камеры QR и фото проходят отдельную release-проверку на устройстве. Обновление на четырёх вкладках подтверждено отдельными настоящими ADB-жестами и новым временем «Данные обновлены» в UI.
- Общий instrumentation-прогон с жестами не считается пройденным: состояние вкладки изменилось во время сценария. При повторении телефона нельзя касаться до окончания теста. Отдельная проверка каждого жеста прошла.
- APK опубликован по HTTPS; скачанная копия совпала по SHA-256 с установленной. Размер: 24396603 байт.

Ниже сохранён отчёт о предыдущем выпуске.

---

# 27G Finora 1.3.0 — проверка 25 сентября 2026

Артефакт: `finora-1.3.0.apk`, 24,155,967 байт, application ID `work.gadmin.finora`, versionCode `8`, Android API 26+ / target 37.

SHA-256 APK: `cc15818e007268834e2cacbef5f38474e16c8d2972ba306c0d83c3c7b7382f21`.

SHA-256 сертификата: `3a3c14d603cc9cfeff5092ece5101acc60512787e90f1330292d98e4f45ee6c0` — тот же ключ, что у предыдущих версий.

## Проверки сборки и устройства

- Kotlin format, 14 unit tests, release lint и R8-сборка — успешно.
- `apksigner verify`: действительная подпись; три ML Kit reflection-конструктора сохранены после R8.
- APK установлен поверх 1.2.1 на Pixel 8 / Android 17 по Wi-Fi ADB. Сессия и данные приложения сохранены.
- QR-сканер и фотокамера открываются без вылета: проверены в подписанной release-сборке.
- На телефоне проверен полный сценарий в изолированной QA-организации: HTTPS-страница парковочного чека → снимок и текст → серверный черновик → исправление магазина и суммы строки → явное подтверждение → расход 30,00 MDL. Пользовательская организация Home для тестового расхода не использовалась.
- Визуально проверены главный экран, карточка чека и редактор. Переключения вкладок анимированы; Compose использует системный масштаб анимаций.
- Публичный APK скачан обратно по HTTPS, его байты совпадают с установленной сборкой.

## Сервер и веб

- На изолированной PostgreSQL прошли 129 tests и 11 subtests. Ruff lint/format, TypeScript, ESLint и Vite build — успешно.
- Регрессии TOTAL: сумма справа от товарного блока, полный исходник и перекрытие фрагментов, пустой/нулевой/ошибочный итог, запрет подмены наличными/сдачей/налогом/скидкой, один дополнительный AI-запрос и сохранение черновика при ошибке провайдера.
- Существующие проверки запрещают загрузку сайтов сервером для `phone_page` и фото с `resolve_qr=false`. Сохранение исправленного чека и изоляция организаций остаются покрыты тестами.
- Веб проверен в браузере при 1280×720 и 390×844: вход, выбор организации, обзор, доходы, покупки, настройки и форма операции. Горизонтального переполнения на 390 px нет, ошибок JavaScript не обнаружено. Escape закрывает выбор организации и возвращает фокус. CSS и графики учитывают `prefers-reduced-motion`.

## Проверка опубликованной версии

- Код `8b5da95e9b626781a020901a7a885a79a3fd3167`: [server CI](https://github.com/gadmin2151/Finora/actions/runs/36181182211) и [Android CI](https://github.com/gadmin2151/Finora/actions/runs/36181182159) завершились успешно.
- Сервер на `https://finora.gadmin.work` обновлён через Compose. Перед обновлением создана зашифрованная копия `finora-before-update-20260925-194718.tar.gz.age`. Образ AMD64/ARM64: `ghcr.io/gadmin2151/finora@sha256:049581deb6275bf0392737f141a4e331a247341e6bed461531fd8e755120c609`. БД и API healthy; схема БД прежняя.
- На обновлённом сервере выполнено настоящее распознавание предоставленных фотографий в отдельной QA-организации с настроенным OpenAI: Linella — **235,08 MDL, 11 строк, сумма строк 235,08**; парковка — **30,00 MDL, 1 строка, сумма строки 30,00**. Оба результата остались черновиками для проверки, без создания расхода.
- Временные тестовые организации, пользователь, чеки и расходы удалены. Рабочая история Home не менялась.

## Ограничения

Распознавание остаётся вероятностным: отсутствие напечатанной суммы нельзя исправить вычислением или догадкой. Неуверенные поля и скидки требуют проверки. Старый QR Linella/SIFT из предоставленного примера открывает форму проверки без товаров и итога — для него нужен снимок бумажного чека. Улучшения фото не меняют содержимое сайта провайдера.
